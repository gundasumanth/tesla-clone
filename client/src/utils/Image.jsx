import React from 'react'

const Image = ({src}) => {
  return (
    <div className="image absolute bottom-[50rem] z-[-1]">
        <img  className=""src={src} alt="telsa model y car"/>
    </div>
  )
}

export default Image